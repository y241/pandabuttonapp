version: '3'
services:
  webserver:
    build: ./
    image: node-express-dev:1.0
    container_name: pandaApp
    tty: true
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8080:3001"
    command: npm start